// ─── MODELS ───────────────────────────────────────────────
import 'package:flutter/material.dart';

class AboutItem {
  final IconData icon;
  final String title;
  final String description;
  const AboutItem(this.icon, this.title, this.description);
}
